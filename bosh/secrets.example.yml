# These are secrets because the volume names need to be secret,
# or a customer will see this and be able to mount up another
# customer's volumes.  These should probably be something like a uuid.
nfstestserver-volumes:
  - XXX

# we are making this live here because it seems like a good idea to not
# publicize customer names.  This is a list of cf orgs that have this
# service enabled for it.
service-organization-development: XXX

